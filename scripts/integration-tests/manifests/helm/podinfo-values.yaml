# Helm values for podinfo direct deploy test
replicaCount: 1

image:
  repository: ghcr.io/stefanprodan/podinfo
  tag: ""

service:
  enabled: true

ingress:
  enabled: false

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi
